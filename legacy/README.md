# Legacy experiments

This directory preserves the original ARIMA, SARIMAX, LSTM, and notebook experiments.

They are historical material only. Several dependencies and APIs are obsolete, and the modelling strategy no longer represents the maintained direction of the repository.

Maintained code belongs under `src/count_time_series/`.
